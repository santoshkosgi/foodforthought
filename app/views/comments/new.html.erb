<h1>new</h1>
<%= form_for([@hotel, @hotel.comments.build]) do |f| %>
  <div class="field">
    <%= f.label :commenter %>
    <div class="input">
      <%= f.text_field :commenter %>
    </div>
  </div>
<br/>
<br/>
  <div class="field">
    <%= f.label :body %>
    <div class="input">
      <%= f.text_area :body %>
    </div>
  </div>
  <div class="actions">
    <%= f.submit :class=>"btn primary"%>
  </div>
    <ul class="pager">
      <li>
        <%= link_to "back" ,@hotel %>
      </li>
    </ul>

<% end %>
